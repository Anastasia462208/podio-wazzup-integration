{
  "name": "podio-wazzup-test-webhook",
  "version": "1.0.0",
  "description": "Test webhook server for Podio-Wazzup integration",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "test": "curl -X POST http://localhost:3000/webhook/test -H 'Content-Type: application/json' -d '{\"test\": true}'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "webhook",
    "podio",
    "wazzup",
    "integration",
    "whatsapp",
    "telegram"
  ],
  "author": "Podio-Wazzup Integration",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}
